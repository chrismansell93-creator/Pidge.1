import { LegalPage } from "@/components/legal-page";

export const metadata = { title: "Support — Pidge" };

export default function SupportPage() {
  return (
    <LegalPage title="Support">
      <p>Email support@pidge.dating for account, billing, or safety help. For Android billing, also use Google Play subscriptions.</p>
      <p>To delete your account: open the app, go to Me, then Delete account. That removes you from the nearby grid.</p>
    </LegalPage>
  );
}
